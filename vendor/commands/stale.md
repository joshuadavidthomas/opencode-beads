---
description: Show stale issues not updated recently
argument-hint: [--days <N>]
---
Run `bd stale --json` to find issues not updated recently.

Default is 30 days. Use `--days 7` for shorter window.

Present stale issues with:
- Last update date
- Current status
- Suggested action (review, close, update)
